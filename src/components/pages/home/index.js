import React from 'react';
import Directory from '../../../directory';
import './styles.scss';

const Home = (props) => {
  return (
    <section className='home'>
      <Directory />
    </section>
  );
};

export default Home;
